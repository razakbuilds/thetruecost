"use client";
import { Share2, AlertTriangle, Check, ArrowUpRight } from "lucide-react";
import { naira } from "@/lib/format";
import type { Estimate } from "@/lib/types";

export default function EstimateResult({ estimate }: { estimate: Estimate }) {
  const text = `My TrueCost estimate: ${estimate.eventType} in ${estimate.city}, ${estimate.guestCount} guests, ${estimate.styleTier} style. Total: ${naira(estimate.total)}. ${estimate.lines.map(x=>`${x.category}: ${naira(x.amount)} (${x.pct_of_total}%)`).join(" · ")} Illustrative estimate, not a quote or guarantee.`;
  const wa = `https://wa.me/?text=${encodeURIComponent(text)}`;
  return <section id="estimate-result" className="border-y border-[#ded8ca] bg-[#efeade] px-5 py-16 md:px-8"><div className="mx-auto max-w-5xl"><div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.18em] text-[#8a7a47]">Your starting budget</p><h2 className="display text-4xl font-semibold text-[#0F2A22] md:text-5xl">The TrueCost estimate</h2><p className="mt-2 text-sm text-[#68726c]">{estimate.eventType} · {estimate.city} · {estimate.guestCount} guests · {estimate.styleTier}</p></div><a href={wa} target="_blank" rel="noreferrer" className="inline-flex items-center justify-center gap-2 rounded-xl border border-[#0F2A22] bg-white px-4 py-3 text-sm font-bold text-[#0F2A22] hover:bg-[#f8f8f5]"><Share2 className="h-4 w-4"/> Share on WhatsApp</a></div>
  <div className="grain paper-shadow rounded-2xl border border-[#d6ccb9] bg-[#FBF6EA] p-5 sm:p-8"><div className="relative z-10"><div className="flex items-end justify-between gap-4 border-b-2 border-[#0F2A22]/70 pb-6"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-[#827762]">Estimated total</p><p className="display mt-1 text-4xl font-bold text-[#0F2A22] sm:text-5xl">{naira(estimate.total)}</p></div><div className="text-right text-xs text-[#81796b]">{estimate.lines.length} budget categories</div></div><div className="py-2">{estimate.lines.map((line)=><div key={line.category} className="ledger-rule flex items-center gap-3 py-4"><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><span className="truncate text-sm font-semibold text-[#26332d]">{line.category}</span>{line.risk && <span className="inline-flex items-center gap-1 rounded-full bg-[#f4e9c7] px-2 py-0.5 text-[10px] font-bold text-[#755e13]"><AlertTriangle className="h-3 w-3"/> high attention</span>}</div><div className="mt-1 text-xs text-[#8a877f]">{line.pct_of_total}% of budget</div></div><div className="hidden flex-1 border-b border-dotted border-[#aaa18f] sm:block"/><div className="text-right"><div className="font-mono text-sm font-bold text-[#0F2A22]">{naira(line.amount)}</div></div></div>)}</div><div className="mt-4 rounded-xl bg-[#0F2A22] p-4 text-sm leading-6 text-white/75"><div className="flex gap-3"><Check className="mt-1 h-4 w-4 shrink-0 text-[#C9A227]"/><p><strong className="text-white">Use this as your baseline.</strong> The category splits come from TrueCost benchmark data and are intended to give you a realistic starting point, not a vendor quote.</p></div></div></div></div>
  <div className="mt-5 flex items-start gap-3 text-xs leading-5 text-[#716d64]"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0"/><p><strong>Illustrative estimate:</strong> This is a benchmark-based planning reference, not a quote, valuation, or financial guarantee. Actual vendor prices can vary by date, supplier, scope and negotiation.</p></div><div className="mt-8 flex items-center justify-center gap-2 text-xs font-semibold text-[#0F2A22]">Want to track real quotes against this baseline? <span className="inline-flex items-center gap-1">TrueCost Pro <ArrowUpRight className="h-3.5 w-3.5"/></span></div></div></section>;
}
